type: object
properties:
  base:
    type: string
    example: 10.128.0.2
    description: Base address for the IP source
  count:
    type: integer
    example: 1
    description: Number of IPs to count from the base
