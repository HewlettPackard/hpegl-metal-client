type: object
properties:
  Fragments:
    type: array
    items:
      $ref: ./UpdateIPRange.yaml
    description: List of fragments for the IP pool
  Stats:
    $ref: ./UpdateIPPoolStats.yaml
    description: Statistics for the IP pool
