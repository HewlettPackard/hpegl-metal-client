type: object
required: 
  - Fragments
  - Stats
properties:
  Fragments:
    type: array
    items:
      $ref: ./IPRange.yaml
    description: List of fragments for the IP pool
  Stats:
    $ref: ./IPPoolStats.yaml
    description: Statistics for the IP pool
